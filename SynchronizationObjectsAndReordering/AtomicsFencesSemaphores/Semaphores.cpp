#include "ClassDeclarations.h"

//Microsoft does not have a native version of a semaphore

//Good read on semaphores:
	//He creates his own/shows the idea behind a semaphore, since Microsoft doesn't have a native implementation
	//http://austingwalters.com/multithreading-semaphores/
		//Also, if it wasn't clear the current variable is the semaphore
			//Remembering that what makes it a semaphore is its usage